import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { DesignTokenDocBlock } from 'storybook-design-token';
import CustomDesignTokenDocBlock from '../../../utils/CustomDesignTokenDocBlock';

import * as semanticTokens from '../../../built-tokens/js/semantic-tokens';

<Meta title="Design Tokens/Tokens/Semantic/Cores" />

# Tokens Semânticos

# &nbsp;

# 🚨🚨🚨 Atenção 🚨🚨🚨

<div
  style={{
    padding: 24,
    backgroundColor: '#fef5d5',
    borderRadius: 8,
  }}
>
  ### ⚠️⚠️⚠️ Os tokens semânticos ainda NÃO estão prontos para uso! ⚠️⚠️⚠️

---

Por enquanto, essa página é somente para consulta dos tokens brutos correspondente. Caso você encontre uma spec no figma com um token semântico, **use o token bruto - ou core token - correspondente, no tema claro**

</div>

# &nbsp;

## Color

<CustomDesignTokenDocBlock
  blockType="table"
  previewType="semantic-color"
  presenter="color"
  tokens={Object.keys(semanticTokens).map((semanticToken) => {
    return { name: semanticToken, value: semanticToken };
  })}
  categories={[
    'DSA_COLOR_BACKGROUND_',
    'DSA_COLOR_TEXT_',
    'DSA_COLOR_BORDER_',
    'DSA_COLOR_SPECIALPAGES_',
    'DSA_COLOR_SUBJECTS_',
    'DSA_COLOR_CONTENTBOX_',
    'DSA_COLOR_HIGHLIGHT_',
    'DSA_COLOR_BUTTON_',
    'DSA_COLOR_FEEDBACK_',
  ]}
  useLeftCopyIcon
/>
